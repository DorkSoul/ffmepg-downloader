# Universal Video Downloader Environment Configuration

# Auto-close browser delay in seconds
AUTO_CLOSE_DELAY=15

# Download directory (where videos are saved)
DOWNLOAD_DIR=/app/downloads

# Chrome user data directory (cookies and session data)
CHROME_USER_DATA_DIR=/app/chrome-data

# Flask environment (production or development)
FLASK_ENV=production

# Display server
DISPLAY=:99
