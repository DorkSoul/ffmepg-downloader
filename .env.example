# Video Downloader Configuration
# Copy this file to .env if you want to use environment variables
# Or set these in docker-compose.yml

# Download directory inside container
DOWNLOAD_PATH=/downloads

# Seconds to wait before auto-closing Chrome window after download starts
CHROME_TIMEOUT=15

# Default quality preference (1080, 720, 480, or "best")
DEFAULT_QUALITY=1080

# Display settings (don't change unless you know what you're doing)
DISPLAY=:99
